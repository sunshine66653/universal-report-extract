# 核心
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0
pypdf>=4.0.0
Pillow>=10.0.0
# PDF 页面渲染（对照 HTML）
pymupdf>=1.24.0
# MD 渲染 / Word 输出
markdown2>=2.4.0
python-docx>=1.1.0
# 可选：本地 Docling 解析（重依赖，默认走云端 MinerU 不需要）
# docling>=2.0.0
